<Window x:Class="RPG.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RPG"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="140" />
        </Grid.RowDefinitions>

        <Canvas x:Name="GameCanvas" Background="Black" ClipToBounds="True" Grid.Row="0">
            <!-- Layer pour le sol -->
            <Canvas x:Name="GroundLayer" Panel.ZIndex="0" />
            
            <!--<Canvas x:Name="TilesLayer" Panel.ZIndex="0" /> -->

            <!-- Layer pour les entités (joueur, ennemis) : placé au-dessus des tuiles -->
            <Canvas x:Name="EntitiesLayer" Panel.ZIndex="1" />
        </Canvas>

        <Border x:Name="SpeechPanel" Grid.Row="1" Background="#CC000000" Padding="12" Visibility="Collapsed" Panel.ZIndex="2">
            <TextBlock x:Name="SpeechTextBlock" Foreground="White" FontSize="16" TextWrapping="Wrap" />
        </Border>

        <!-- Menu principal superposé par-dessus le jeu -->
        <local:Menu x:Name="MainMenu" Panel.ZIndex="10" Grid.Row="0" Grid.RowSpan="2" />
    </Grid>
</Window>
